{
  "name": "my-node-project",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --coverage --testResultsProcessor=jest-junit --verbose",
    "test:debug": "jest --coverage --testResultsProcessor=jest-junit --verbose --no-cache --detectOpenHandles",
    "build": "webpack --mode production"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0",
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.0"
  }
}